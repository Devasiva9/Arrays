class Solution {
    public int missingNumber(int[] nums) {
        int n = nums.length;
        int expected = n*(n+1)/2;
        int weget = 0;
        for(int i : nums)
        {
            weget += i;
        }
        return expected-weget;
    }
}
